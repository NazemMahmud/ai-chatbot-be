# Core
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0

# Database
sqlalchemy[asyncio]==2.0.46
asyncpg==0.31.0
alembic==1.18.3
pgvector==0.4.2


# Redis & Background Jobs
arq==0.27.0
redis==5.3.1


# HTTP Client
httpx==0.28.1

# File Upload & Storage
python-multipart==0.0.22
aiofiles>=24.0.0

# Document Parsing - Simple parser (PARSER_TYPE=simple)
pypdf==6.6.2
python-docx==1.2.0

# Document Parsing - Docling Mode (optional, heavy)


# RAG Pipeline - Reranker (cross-encoder, ~80MB model, CPU-only)
sentence-transformers>=3.0.0

# RAG Pipeline - Entity Extraction (NER, ~15MB model, CPU-only)
spacy>=3.7.0

# Auth
PyJWT==2.11.0
pwdlib[argon2]

# Utilities